import SectionPage from "./SectionPage";

export default function Announcements() {
  return (
    <SectionPage
      sectionKey="announcements"
      fallbackTitle="Quick Announcements"
      fallbackBody="Stay up to date with the latest wellness news, upcoming events, and important reminders from your health coach."
    />
  );
}

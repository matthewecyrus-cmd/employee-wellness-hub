import SectionPage from "./SectionPage";

export default function Resources() {
  return (
    <SectionPage
      sectionKey="resources"
      fallbackTitle="Tip Sheets & Resources"
      fallbackBody="Access this month's handouts, tip sheets, and wellness resources. New content is added each month to support your physical health, mental well-being, and daily habits."
    />
  );
}
